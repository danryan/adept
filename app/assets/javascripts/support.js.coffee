jQuery ->
  $(document).foundationTopBar()
  $(document).foundationNavigation()
  $(document).foundationButtons()
  # $(document).foundationCustomForms()
  $(document).foundationAlerts()

  # $('form .custom.dropdown').addCl