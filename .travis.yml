language: ruby
rvm:
  - 1.9.3
bundler_args: --without local
script: 
  - bundle exec rake spec:ci
before_install:
  - sudo apt-get install libarchive1 libarchive-dev
before_script:
  - createuser -s adept
  - rake db:create:all
  - rake db:test:prepare
branches:
  only:
    - master